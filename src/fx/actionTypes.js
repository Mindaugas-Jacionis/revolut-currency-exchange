import { NAME } from './constants';

export const SAMPLE = `${NAME}.SAMPLE`;

export const GET_RATES = `${NAME}.GET_RATES`;
export const GET_RATES_SUCCESS = `${NAME}.GET_RATES_SUCCESS`;
export const GET_RATES_FAILURE = `${NAME}.GET_RATES_FAILURE`;

export const SET_BASE_CURRENCY = `${NAME}.SET_BASE_CURRENCY`;
